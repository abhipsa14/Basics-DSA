#include<iostream>
using namespace std;
int main(){
    int arr[size]={2,3,4};
    int el,pos,i;
    cout<<"Enter position and element:\n";
    cin>>el>>pos;
    if(pos<=5&&pos>=0){
        for(i=5;i>pos;i--)
         arr[i]=arr[i-1];
        arr[pos]=el;
        for(i=0;i<=)
    }
}